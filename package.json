{
    "name": "qa-task-deleonlevin-jiffy",
    "version": "1.0.0",
    "scripts": {
        "test:dev": "ENV=DEV npx playwright test",
        "test:qa": "ENV=STAGING npx playwright test",
        "test:prod": "ENV=PROD npx playwright test",
        "test:ci": "ENV=$ENV npx playwright test",
        "test:local:headless": "ENV=local npx playwright test",
        "test:local:headed": "ENV=local npx playwright test --headed",
        "test:headed": "npx playwright test --headed",
        "codegen": "npx playwright codegen"
    },
    "devDependencies": {
        "@playwright/test": "^1.43.0",
        "dotenv": "^16.5.0",
        "typescript": "^5.0.0"
    }
}
